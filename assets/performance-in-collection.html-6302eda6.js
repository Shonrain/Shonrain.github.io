import{_ as a,o as s,c as n,b as e}from"./app-fcfb95bc.js";const o="/miya-design/assets/set-59e1f58b.png",c="/miya-design/assets/red-black-tree-b33c2fb9.png",p="/miya-design/assets/hash-set-41add210.png",t="/miya-design/assets/map-74b2ccc7.png",l="/miya-design/assets/seq-143f3140.png",d="/miya-design/assets/vector-trie-a69e083b.png",i="/miya-design/assets/list-4315f1bc.png",u={},r=e('<h2 class="blog-title">如何在 Scala 中科学地操作 collection（二）：集合性能比较</h2><p>在平时使用集合的时候，我们经常会选择 Scala 中通用的集合，例如：<code>Seq</code>、<code>Map</code>、<code>List</code>等等，有的时候选择「通用集合」完全可以解决问题，但是当集合操作变得很复杂以至于涉及到「性能问题」的时候，采用「通用集合」可能并不是一个好的选择。在不同的场景下选择合适的集合可以使我们对于集合的操作更加高效。</p><p>大部分情况下，我们都会优先采用「不可变集合」，所以本文将通过比较几种常见的「不可变集合」来阐述各个集合之间的性能差异。</p><h2 id="set" tabindex="-1"><a class="header-anchor" href="#set" aria-hidden="true">#</a> Set</h2><p><img src="'+o+`" alt="Image of Set"></p><p>通过上图可以看到，两种常用的类型：<code>TreeSet</code>、<code>HashSet</code> 都继承至 <code>Set</code>。</p><h3 id="treeset" tabindex="-1"><a class="header-anchor" href="#treeset" aria-hidden="true">#</a> TreeSet</h3><p><code>TreeSet</code> 是用「红黑树」来实现的，「红黑树」是一种相对平衡的二叉查找树，它可以在 <code>O(log2 n)</code> 时间复杂度内做查找，例如：</p><div class="language-scala line-numbers-mode" data-ext="scala"><pre class="language-scala"><code><span class="token keyword">val</span> s <span class="token operator">=</span> scala<span class="token punctuation">.</span>collection<span class="token punctuation">.</span>immutable<span class="token punctuation">.</span>TreeSet<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">4</span><span class="token punctuation">,</span> <span class="token number">5</span><span class="token punctuation">,</span> <span class="token number">7</span><span class="token punctuation">,</span> <span class="token number">8</span><span class="token punctuation">,</span> <span class="token number">11</span><span class="token punctuation">,</span> <span class="token number">14</span><span class="token punctuation">,</span> <span class="token number">15</span><span class="token punctuation">)</span>
s<span class="token operator">:</span> scala<span class="token punctuation">.</span>collection<span class="token punctuation">.</span>immutable<span class="token punctuation">.</span>TreeSet<span class="token punctuation">[</span><span class="token builtin">Int</span><span class="token punctuation">]</span> <span class="token operator">=</span> TreeSet<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">4</span><span class="token punctuation">,</span> <span class="token number">5</span><span class="token punctuation">,</span> <span class="token number">7</span><span class="token punctuation">,</span> <span class="token number">8</span><span class="token punctuation">,</span> <span class="token number">11</span><span class="token punctuation">,</span> <span class="token number">14</span><span class="token punctuation">,</span> <span class="token number">15</span><span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>则其对应的红黑树为：</p><p><img src="`+c+`" alt="image of redBlackTree"></p><p>从上面「红黑树」的结构可以看到在对 <code>TreeSet</code> 进行查找或者修改操作时，其时间复杂度为 <code>O(log2 n)</code>。</p><h3 id="hashset" tabindex="-1"><a class="header-anchor" href="#hashset" aria-hidden="true">#</a> HashSet</h3><p><code>HashSet</code> 是用 <code>Hash Trie</code> 来实现的，从表现形式上可以将 <code>HashSet</code> 看作是一种树结构，该树的每个节点包含32个元素或者32个子树，每个节点都存储相应的 <code>hashcode</code> ，为了方便描述这种结构我们先定义一个 <code>HashSet</code> 的实例，并将该实例用图表现出来：</p><div class="language-scala line-numbers-mode" data-ext="scala"><pre class="language-scala"><code>
scala<span class="token operator">&gt;</span> <span class="token keyword">val</span> s <span class="token operator">=</span> scala<span class="token punctuation">.</span>collection<span class="token punctuation">.</span>immutable<span class="token punctuation">.</span>HashSet<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">3</span><span class="token punctuation">,</span> <span class="token number">33</span><span class="token punctuation">,</span> <span class="token number">35</span><span class="token punctuation">,</span> <span class="token number">50</span><span class="token punctuation">,</span> <span class="token number">289</span><span class="token punctuation">,</span> <span class="token number">306</span><span class="token punctuation">,</span> <span class="token number">1057</span><span class="token punctuation">)</span>
s<span class="token operator">:</span> scala<span class="token punctuation">.</span>collection<span class="token punctuation">.</span>immutable<span class="token punctuation">.</span>HashSet<span class="token punctuation">[</span><span class="token builtin">Int</span><span class="token punctuation">]</span> <span class="token operator">=</span> Set<span class="token punctuation">(</span><span class="token number">289</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">1057</span><span class="token punctuation">,</span> <span class="token number">33</span><span class="token punctuation">,</span> <span class="token number">306</span><span class="token punctuation">,</span> <span class="token number">3</span><span class="token punctuation">,</span> <span class="token number">35</span><span class="token punctuation">,</span> <span class="token number">50</span><span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>看到上面的代码，我们或许会有一个疑问，就是得到的 <code>HashSet</code> 中各个元素的顺序好像变了，这是因为在实现 <code>HashSet</code> 时，元素的顺序不是按照我们给定的顺序来的，而是根据元素对应的 <code>hashcode</code> 来决定的，在 <code>HashSet</code> 中，元素的 <code>hashcode</code>是通过下面的操作得到的：</p><div class="language-scala line-numbers-mode" data-ext="scala"><pre class="language-scala"><code>
<span class="token keyword">def</span> getHashCode<span class="token punctuation">(</span>key<span class="token operator">:</span> <span class="token builtin">Int</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token punctuation">{</span>
    <span class="token keyword">val</span> hcode <span class="token operator">=</span> key<span class="token punctuation">.</span>##
    <span class="token keyword">var</span> h<span class="token operator">:</span> <span class="token builtin">Int</span> <span class="token operator">=</span> hcode <span class="token operator">+</span> <span class="token operator">~</span><span class="token punctuation">(</span>hcode <span class="token operator">&lt;&lt;</span> <span class="token number">9</span><span class="token punctuation">)</span>
    h <span class="token operator">=</span> h <span class="token operator">^</span> <span class="token punctuation">(</span>h <span class="token operator">&gt;&gt;&gt;</span> <span class="token number">14</span><span class="token punctuation">)</span>
    h <span class="token operator">=</span> h <span class="token operator">+</span> <span class="token punctuation">(</span>h <span class="token operator">&lt;&lt;</span> <span class="token number">4</span><span class="token punctuation">)</span>
    h <span class="token operator">^</span> <span class="token punctuation">(</span>h <span class="token operator">&gt;&gt;&gt;</span> <span class="token number">10</span><span class="token punctuation">)</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>为了方便理解，我们这里规定元素的 <code>hashcode</code> 就是它本身，那么之前的代码就变成了：</p><div class="language-scala line-numbers-mode" data-ext="scala"><pre class="language-scala"><code>
scala<span class="token operator">&gt;</span> <span class="token keyword">val</span> s <span class="token operator">=</span> scala<span class="token punctuation">.</span>collection<span class="token punctuation">.</span>immutable<span class="token punctuation">.</span>HashSet<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">3</span><span class="token punctuation">,</span> <span class="token number">33</span><span class="token punctuation">,</span> <span class="token number">35</span><span class="token punctuation">,</span> <span class="token number">50</span><span class="token punctuation">,</span> <span class="token number">289</span><span class="token punctuation">,</span> <span class="token number">306</span><span class="token punctuation">,</span> <span class="token number">1057</span><span class="token punctuation">)</span>
s<span class="token operator">:</span> scala<span class="token punctuation">.</span>collection<span class="token punctuation">.</span>immutable<span class="token punctuation">.</span>HashSet<span class="token punctuation">[</span><span class="token builtin">Int</span><span class="token punctuation">]</span> <span class="token operator">=</span> Set<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">33</span><span class="token punctuation">,</span> <span class="token number">1057</span><span class="token punctuation">,</span> <span class="token number">289</span><span class="token punctuation">,</span> <span class="token number">3</span><span class="token punctuation">,</span> <span class="token number">35</span><span class="token punctuation">,</span> <span class="token number">50</span><span class="token punctuation">,</span> <span class="token number">306</span><span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>其对应的树结构为： <img src="`+p+'" alt="image of hashSet"></p><p>通过上图，我们可以看到「树」的每个节点都存储相应的 <code>hashcode</code>，在这棵「树」上查找某个值时，首先用该元素对应的 <code>hashcode</code> 的最后 <code>5</code> 个 <code>bit</code> 查找第一层「子树」，然后毎 <code>5</code> 个 <code>bit</code> 找到下一层 「子树」。当存储在一个节点中所有元素的代表他们当前所在层的 <code>hashcode</code> 位都不相同时，查找结束。例如：</p><p>如果我们要查找数字 <code>1057</code> 是否在这棵「树」上面：</p><ol><li><p>将 <code>1057</code> 转换为 「二进制」，我们得到 <code>00001 00001 00001</code>，然后取出最后的 <code>5</code> 个 <code>bit</code>：<code>00001</code>；</p></li><li><p>查找第一层「子树」，找到 <code>00001</code> 对应的节点，该节点有三个「孩子」，所以我们要进入下一层，接下来取出第二个「五位」：<code>00001</code>；</p></li><li><p>查找第二层「子树」，找到 <code>00001</code> 对应的节点，该节点有两个「孩子」，所以我们要进入下一层，接下来取出第三个「五位」：<code>00001</code>；</p></li><li><p>查找第三层「子树」，找到 <code>00001</code> 对应的节点，该节点就只有一个元素 <code>1057</code>，所以我们找到了。</p></li></ol><p>在这棵树中，我们查询 <code>1057</code> 的时间复杂度为 <code>O(3)</code>，由于 <code>Hashset</code> 中的每一个节点都可以有 <code>32</code> 个分支，所以其在查询或者修改等操作时的效率会大大提高，例如：对于一个拥有100万个元素的 <code>HashSet</code>，我们只需要四层节点。（因为10<sup>6</sup> ≈ 32<sup>4</sup>），我们在查询其中的某一个元素时，最多只需要 <code>O(4)</code> 的时间复杂度，而采用 <code>TreeSet</code> 就需要 <code>O(20)</code> 的时间复杂度，所以在不出现「哈希碰撞」的情况下（在日常开发中使用 <code>HashSet</code> 极少会出现「哈希碰撞」），<code>HashSet</code> 的随机访问时间复杂度为 <code>log32 n</code>，比前面介绍的 <code>TreeSet</code> 要好。</p><h3 id="总结" tabindex="-1"><a class="header-anchor" href="#总结" aria-hidden="true">#</a> 总结</h3><p>通过前面我们对两种 <code>Set</code> 的比较，我们可以得出：</p><ol><li><p>当集合中元素不是很多，而且对效率要求不高的时候，选择通用的 <code>Set</code> 就可以解决问题；</p></li><li><p>当元素数量非常庞大，并且对效率要求比较高的时候，我们一般选择 <code>HashSet</code>；</p></li><li><p>当选择 <code>HashSet</code> 时，出现很严重的 「哈希碰撞」时，采用 <code>TreeSet</code>。</p></li></ol><h2 id="map" tabindex="-1"><a class="header-anchor" href="#map" aria-hidden="true">#</a> Map</h2><p><img src="'+t+'" alt="Image of Map"></p><p>如上图所示，<code>Map</code> 支持三种类型：<code>HashMap</code>、<code>TreeMap</code> 和 <code>ListMap</code>，其中比较常用的是前面两种。</p><h3 id="hashmap、treemap" tabindex="-1"><a class="header-anchor" href="#hashmap、treemap" aria-hidden="true">#</a> HashMap、TreeMap</h3><p><code>HashMap</code> 与我们前面提到的 <code>HashSet</code> 结构类似，同样，<code>TreeMap</code> 与 <code>TreeSet</code> 结构类似，一般情况下，优先选择 <code>HashMap</code>。</p><h3 id="listmap" tabindex="-1"><a class="header-anchor" href="#listmap" aria-hidden="true">#</a> ListMap</h3><p><code>ListMap</code> 是一种「链表」结构，在对其中的元素进行操作的时候，我们通常都会去遍历其中的元素，所以其查询、修改等操作的时间复杂度也同列表长度成「线性关系」，一般情况下，在 <code>Scala</code> 中，我们很少使用 <code>ListMap</code>，只有当 <code>Map</code> 中处在前面的元素的访问频率远远大于处在后面的元素时，才会采用 <code>ListMap</code>。</p><h3 id="总结-1" tabindex="-1"><a class="header-anchor" href="#总结-1" aria-hidden="true">#</a> 总结</h3><ol><li><p>当集合中元素不是很多，而且对效率要求不高的时候，选择通用的 <code>Map</code> 就可以解决问题</p></li><li><p>当元素数量非常庞大，并且对效率要求比较高的时候，我们一般选择 <code>HashMap</code>；</p></li><li><p>当选择 <code>HashSet</code> 时，出现很严重的 「哈希碰撞」时，采用 <code>TreeMap</code>；</p></li><li><p>当 <code>Map</code> 中处在前面的元素的访问频率远远大于处在后面的元素时，采用 <code>ListMap</code>。</p></li></ol><h2 id="seq" tabindex="-1"><a class="header-anchor" href="#seq" aria-hidden="true">#</a> Seq</h2><p><img src="'+l+`" alt="Image of Seq"></p><p>通过上图可以看到，两种常用的类型：<code>Vector</code>、<code>List</code> 都继承至 <code>Seq</code></p><h3 id="vector" tabindex="-1"><a class="header-anchor" href="#vector" aria-hidden="true">#</a> Vector</h3><p><code>Vector</code> 的结构与我们前面提到的 <code>HashSet</code> 非常的类似，我们可以将 <code>Vector</code> 看成是由元素的「下标」组成的「前缀树」，该树的每个节点也包含32个元素或者32个子树，每个节点存储相应下标对应的元素以及具有相同「前缀」的「孩子」，为了方便描述，我们依然先定义一个 <code>Vector</code> 的实例：</p><div class="language-scala line-numbers-mode" data-ext="scala"><pre class="language-scala"><code>
scala<span class="token operator">&gt;</span> <span class="token keyword">val</span> v <span class="token operator">=</span> <span class="token punctuation">(</span><span class="token number">0</span> to <span class="token number">1057</span><span class="token punctuation">)</span><span class="token punctuation">.</span>toVector
v<span class="token operator">:</span> Vector<span class="token punctuation">[</span><span class="token builtin">Int</span><span class="token punctuation">]</span> <span class="token operator">=</span> Vector<span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">3</span><span class="token punctuation">,</span> <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span> <span class="token punctuation">,</span> <span class="token number">1057</span><span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>我们定义了一个具有 <code>1058</code> 个元素的 <code>Vector</code>，每一个元素的下标与该元素的值相等。接下来我们用图将该实例表现出来：</p><p><img src="`+d+`" alt="Image of vector trie"></p><p>上图展示了实例中的部分元素，可以看到具有相同「前缀」的元素拥有相同的「父亲」，例如：</p><p>元素 <code>33</code>、<code>35</code>、<code>50</code>对应的「二进制」分别是：<code>00001 00001</code>、<code>00001 00011</code>、<code>00001 10010</code>，它们的「高五位」也就是「前缀」都是 <code>00001</code>。</p><p>现在我们查找其中的某个元素 <code>1057</code>：</p><ol><li><p><code>1057</code> 对应的下标是 <code>1057</code>，转换为二进制为：<code>00001 00001 00001</code>；</p></li><li><p><code>1057</code> 最高五位也就是第一个前缀为 <code>00001</code>，在第一层「子树」中找到 <code>00001</code> 对应的节点；</p></li><li><p>第二个五位也就是第二个 「前缀」是 <code>00001</code>，则在第二层「子树」中找到 <code>00001</code> 对应的节点；</p></li><li><p>最后一个五位是 <code>00001</code>，在第三层子树中找到 <code>00001</code> 对应的节点，则该元素存在于这个节点中。</p></li></ol><p>可以看到我们查询 <code>1057</code> 的时间复杂度为：<code>O(3)</code>，由于 <code>Vector</code> 也是采用具有 <code>32</code> 分支的树结构，所以它的查询、修改等操作的时间复杂度也是 <code>log32 n</code>，由于下标不会重复，所以不会像 <code>HashSet</code> 那样出现 「哈希碰撞」，所以它的效率比 <code>HashSet</code> 要好。</p><p>在 <code>Scala</code> 中使用集合的时候，如果没有特别的要求，我们应该首先选择 <code>Vector</code>。当然，<code>vector</code> 也有不适用的场景，如果我们要频繁地执行一个集合的「头」和「尾」的操作，选择 <code>Vector</code> 就不太好了，这时我们可以选择 <code>List</code>。</p><h3 id="list" tabindex="-1"><a class="header-anchor" href="#list" aria-hidden="true">#</a> List</h3><p>在日常开发中我们使用 <code>List</code> 的频率非常高，<code>List</code> 是个 「单链表」结构，其中的每个节点都可以看作是一个「格子」，每一个「格子」持有两个引用，一个引用指向值，另一个引用指向后续的元素。</p><div class="language-scala line-numbers-mode" data-ext="scala"><pre class="language-scala"><code>
scala<span class="token operator">&gt;</span> <span class="token keyword">val</span> l <span class="token operator">=</span> List<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">3</span><span class="token punctuation">)</span>
l<span class="token operator">:</span> List<span class="token punctuation">[</span><span class="token builtin">Int</span><span class="token punctuation">]</span> <span class="token operator">=</span> List<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">3</span><span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>其结构用图表示出来为：</p><p><img src="`+i+'" alt="Image of list"></p><p><code>List</code> 只有在操作 「头部」和「尾部」时具有 <code>O(1)</code> 的复杂度，如果列表中的元素非常多，那 <code>List</code> 的效率远远不如前面提到的 <code>Vector</code>，所以只有当我们需要频繁操作集合中的首尾元素时，才去选择 <code>List</code>，大部分情况下， <code>Vector</code> 应该是我们缺省的选择。</p><h3 id="总结-2" tabindex="-1"><a class="header-anchor" href="#总结-2" aria-hidden="true">#</a> 总结</h3><ol><li><p>一般情况下，优先采用 <code>Vector</code>；</p></li><li><p>只有在头尾操作非常频繁的时候选择 <code>List</code>。</p></li></ol>',58),k=[r];function m(h,b){return s(),n("div",null,k)}const g=a(u,[["render",m],["__file","performance-in-collection.html.vue"]]);export{g as default};
